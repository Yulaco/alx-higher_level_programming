#!/usr/bin/python3
# Function replaces an element of a list

def replace_in_list(my_list, idx, element):

    if idx < 0:
        return(my_list)
    else:
        return(my_list)
    for element in my_list:
        return("{}".format(my_list[element]))
