#!/usr/bin/python3
# Removes all characters c and C

def no_c(my_string):

    new_string = ""
    for a in my_string:
        if my_string == 'c' or my_string == 'C':
            my_string.remove(c, C)
    return(my_string)
